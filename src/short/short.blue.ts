import { embeddedCommand } from "../command/command";

export const shortBlue = embeddedCommand(
  "short",
  {
    description: 'Configure a short "bluecodex" such as "blue" or "b"',
  },
  () => {
    // TODO
  },
);
