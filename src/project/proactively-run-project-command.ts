export async function proactivelyRunProjectCommand(): Promise<boolean> {
  // if (name !== initCommand.blueprint.name && !ioc.project.isInitialized) {
  //   console.log();
  //   console.log(`Welcome to ${themedProjectName}\n`);
  //
  //   return askToInitProject();
  // }

  return false;
}
